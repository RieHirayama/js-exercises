# 問題16.6 回答

## 手順
1. `node ch16/ex06/index.js` を実行して `sample.bin` を作成する。
2. `sample.bin` をバイナリエディタ(VSCodeの拡張機能 Hex Editor)で開いて確認する。

## 確認結果
`0x41 0x42 0x43` (ABC) の後ろに拡張された領域が `0x00` で埋められていることを確認した。  
ASCII の `"0"` (0x30) ではなく、ゼロ値 (0x00) が書き込まれる。
